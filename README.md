# SMC Backtester — Vercel deployment

Static deployment package for the confirmed working v10 HTML.

- `public/index.html` is the v10 backtester, copied without modifying its contents.
- No external runtime or server is required.
- CSV data remains client-side in the browser.
